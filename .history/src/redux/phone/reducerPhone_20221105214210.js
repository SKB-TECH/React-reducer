const initialStatePhone = {
    phones: 5
}

const PhoneReducer = (state, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}