const initialStatePhone = {
    phones: 5
}

const PhoneReducer = (state=, action) => {
    switch (action.type) {
        case "BAY_PHONE":
            
            break;
    
        default:
            break;
    }
}