const initialStatePhone = {
    phones: 5
}

const PhoneReducer = (state=initi, action) => {
    switch (action.type) {
        case "BAY_PHONE":
            
            break;
    
        default:
            break;
    }
}